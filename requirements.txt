# Веб-интерфейс (стеклянный дизайн) — основной
pywebview>=5.0        # встроенное окно; на Windows использует WebView2/Edge

# Классический интерфейс на Qt (запуск: python run.py qt) — опционально
PySide6>=6.6

# Импорт списков
openpyxl>=3.1        # чтение/запись .xlsx

# Валидация адресов
email-validator>=2.1 # синтаксис + нормализация; MX-проверка опционально

# Безопасное хранение пароля SMTP в системном хранилище
keyring>=24.0
