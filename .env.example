# Скопируйте в .env и задайте значения:  cp .env.example .env

# Общий пароль на вход в панель (ОБЯЗАТЕЛЬНО). Один на всю команду.
MAILSENDER_ACCESS_PASSWORD=change-me-please

# Пароль от SMTP-ящика (необязательно — можно ввести в интерфейсе вместо этого).
MAILSENDER_SMTP_PASSWORD=
